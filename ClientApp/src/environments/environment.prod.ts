import { FirebaseConfig } from 'src/app/constants/app.constants';

export const environment = {
  baseUrl: 'https://se491-softwareengineeringstudio.github.io/Mindful/',
  production: true,
  firebase: FirebaseConfig
};
